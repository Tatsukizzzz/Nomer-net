total_sum = 0
print("Введите символы. Для завершения просто нажмите Enter.")

while True:
    line = input()
    if line == "":
        break
    for ch in line:
        total_sum += ord(ch)

print("Сумма кодов всех введённых символов:", total_sum)
1234
